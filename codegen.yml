schema: http://localhost:4000
overwrite: true
documents: ./app/graphql/**/*.graphql
generates:
  app/gen/apollo.ts:
    plugins:
      - typescript-common
      - typescript-client
      - typescript-server
      - typescript-apollo-angular
